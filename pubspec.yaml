name: dapps_voting
description: Voting application base on block chain and smart contracts

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  web3dart: any
  http: ^0.13.4
  flutter_bloc: ^8.0.0
  flutter_services_binding: ^0.1.0
  equatable: ^2.0.3
  formz: ^0.4.1
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockingjay: ^0.3.0
  mocktail: ^0.3.0

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/